# local

Local development sandbox for running and testing project features localy.

Includes a WebSocket receiver for event/loggin.


To install dependencies:

```bash
bun install
```

To run:

```bash
bun run dev
```
